# Minimal requirements for HuggingFace Spaces demo
# The demo loads a pre-computed JSONL dataset and renders it with Gradio.
# No model inference, no torch, no heavy data libraries needed.

# Web interface
gradio>=4.44.0,<6.0
